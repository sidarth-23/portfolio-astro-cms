# Environment for running MongoDB and MinIO locally with `task up`.
# Copy to .env when running the backing services only.

S3_BUCKET=sidshub-media
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
MINIO_CREATE_BUCKET_ON_STARTUP=true
