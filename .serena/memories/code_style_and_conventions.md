TypeScript strict; prefer type-only imports with `type`; semicolons required; double quotes preferred. Use `@/` alias for imports from src. Import order: Astro built-ins, third-party, local aliases. Naming: Astro/React components PascalCase; utility files camelCase; routes lowercase; constants SCREAMING_SNAKE_CASE. Astro component structure: frontmatter, template, optional style. Prefer Tailwind + DaisyUI utility classes over custom CSS. Blog posts in src/content/blog/*.mdx must follow schema in src/content/config.ts.