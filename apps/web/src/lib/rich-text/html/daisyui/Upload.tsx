/** @jsxImportSource preact */
import { createUpload } from "@/lib/rich-text/html/shared/createUpload";

export const Upload = createUpload({
  frameStyle: "overflow:hidden;border-radius:0.5rem;",
});
